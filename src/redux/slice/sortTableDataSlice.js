import { createSlice } from "@reduxjs/toolkit";

const initialState = {};
const sortTableDataSlice = createSlice({
  name: "sortData",
  initialState: initialState,
  reducers: {
    sortData: () => {},
  },
});
